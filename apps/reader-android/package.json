{
  "name": "reader-android",
  "version": "1.0.0",
  "private": true,
  "description": "Coquille Android Capacitor — lecteurs + rédaction Wab-infos (charge le site en WebView)",
  "scripts": {
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "cap:run": "cap run android",
    "android:debug": "cap sync android && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.5",
    "@capacitor/core": "^6.0.5",
    "@capacitor/push-notifications": "^6.0.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.5",
    "typescript": "^5"
  },
  "capacitor": {
    "platforms": [
      "android"
    ]
  }
}
