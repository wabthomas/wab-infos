/**
 * Polices définies en CSS (`globals.css`) pour éviter le téléchargement Google Fonts au build.
 * `next/font/google` provoque des ETIMEDOUT quand fonts.googleapis.com est inaccessible.
 */
export const fontVariables = '';
